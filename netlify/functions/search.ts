import type { Handler } from "@netlify/functions"; import type { PageManifestEntry } from "../../src/domain/page"; import { cors } from "./_lib/cors"; import { createDataRepo, toManifestEntry } from "./_lib/dataRepo"; import { requireSession } from "./_lib/requireSession";
export function rankByQuery(entries: PageManifestEntry[], query: string) { const needle = query.trim().toLowerCase(); if (!needle) return []; return entries.filter(entry => [entry.title, entry.excerpt, ...entry.tags].some(value => value.toLowerCase().includes(needle))).sort((a, b) => Number(b.title.toLowerCase().includes(needle)) - Number(a.title.toLowerCase().includes(needle))); }
export const handler: Handler = async event => { const denied = requireSession(event); if (denied) return denied; const query = event.queryStringParameters?.q ?? ""; return { statusCode: 200, headers: cors(), body: JSON.stringify(rankByQuery((await createDataRepo().listPages()).map(toManifestEntry), query)) }; };
